# Speak playground (SvelteKit)

Same UX as the vanilla example: type text, press Enter, toggle enqueue vs interrupt on `Experience.speak()`.

## Run

From this directory:

```bash
npm install
npm run dev
```

Open http://localhost:4005

From repo root: `./start sveltekit` (starts gallery + all SvelteKit examples).
