# Basic embed (vanilla)

```bash
# from repo root
PORT=4001 node scripts/serve-example.mjs examples/basic-embed/vanilla
```

Open **http://localhost:4001**

Uses production Liforma by default. Demo experiences allow `http://localhost:4001`. For your own `exp_…`, add that origin in the developer portal.
