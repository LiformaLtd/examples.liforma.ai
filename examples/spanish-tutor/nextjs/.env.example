# Optional — load lessons from your Liforma project catalog (server-side only).
# Without these, the app uses static fallback lessons from lib/lessons.ts.
# LIFORMA_API_KEY=
# LIFORMA_PROJECT_ID=seed_proj_examples
# LIFORMA_API_URL=https://api.liforma.ai
