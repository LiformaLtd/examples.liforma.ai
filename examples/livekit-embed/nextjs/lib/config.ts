/**
 * Demo content only — not part of the Liforma ↔ LiveKit integration.
 * Integration: copy helloByo.ts → startByoSpeech → connectLiveKitAgent
 */

/** Meet coffee barista — same demo experience as basic embed. */
export const EXPERIENCE_ID = 'exp_01EXAMPLES_COFFEE_BARISTA';

/** Default LiveKit room for this demo. */
export const DEFAULT_ROOM_NAME = 'liforma-coffee-barista';

/** Default participant identity for the browser listener. */
export const DEFAULT_IDENTITY = 'listener';
